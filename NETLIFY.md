# انتشار MissMahta روی Netlify

این مخزن دو مسیر انتشار دارد. `npm run build` مسیر فعلی Sites/Cloudflare را حفظ می‌کند؛ Netlify با فایل `netlify.toml` از `npm run build:netlify` و خروجی `.next` استفاده می‌کند. فایل `tsconfig.netlify.json` پیاده‌سازی ورود و ذخیره‌سازی مخصوص Netlify را انتخاب می‌کند.

## تنظیمات پروژه missmahta

- Repository: `sabervnk/webdesign`
- Branch: `main`
- Base directory: خالی (ریشه مخزن)
- Build command: `npm run build:netlify`
- Publish directory: `.next`
- Node: نسخه ۲۲، طبق `netlify.toml`
- افزونه Next.js خودکار Netlify فعال بماند.

پس از دریافت این اصلاحات، Deploy جدید را بررسی کنید. اگر انتشار خودکار آغاز نشد، در Deploys گزینه Trigger deploy / Deploy site را انتخاب کنید.

## فعال‌کردن ورود مدیر

در Netlify مسیر **Project configuration → Environment variables** را باز کنید و این متغیرها را برای Production و اجرای Functions اضافه کنید:

| کلید | مقدار |
|---|---|
| `MAHELA_ADMIN_PASSWORD` | رمز اختصاصی مدیر، حداقل ۱۶ کاراکتر؛ ترجیحاً رمز تصادفی تولیدشده با password manager |
| `MAHELA_SESSION_SECRET` | رشته تصادفی متفاوت، حداقل ۳۲ کاراکتر؛ ترجیحاً ۶۴ کاراکتر تصادفی |
| `MAHELA_ADMIN_EMAIL` | اختیاری؛ پیش‌فرض `lahzetv@gmail.com` |
| `MAHELA_SITE_URL` | اختیاری؛ برای این پروژه `https://missmahta.netlify.app`؛ پیش‌فرض URL پروژه Netlify |
| `MAHELA_TEACHER_INVITE_CODE` | اختیاری؛ کد دعوت تصادفی حداقل ۱۶ کاراکتر برای ثبت‌نام معلم‌های آزمایشی |

رمزها را در GitHub یا کد سایت وارد نکنید. متغیرهای محرمانه نباید پیشوند `NEXT_PUBLIC_` داشته باشند. بعد از ذخیره متغیرها، یک Deploy جدید انجام دهید.

با ایمیل مدیر و رمز انتخابی خودتان به `/signin` وارد شوید؛ صندوق پیام‌ها در `/admin` است. بدون دو متغیر ضروری، صفحات عمومی قابل نمایش‌اند ولی ورود به پنل بسته می‌ماند و پیام روشنی نشان می‌دهد.

برای تست چند معلم، کد دعوت را تنظیم کنید. هر معلم در `/signin` گزینه ثبت‌نام را انتخاب می‌کند و با ایمیل، رمز شخصی و کد دعوت حساب می‌سازد. کد دعوت را فقط به معلم‌های مورد نظر بدهید. حذف یا تغییر کد، ثبت‌نام‌های بعدی را می‌بندد یا به کد جدید محدود می‌کند. حساب‌های قبلی همچنان می‌توانند وارد شوند.

نشست‌ها حداکثر ۸ ساعت اعتبار دارند. رمز معلم‌ها به‌صورت scrypt hash ذخیره می‌شود. تغییر `MAHELA_SESSION_SECRET` تمام نشست‌های فعلی را باطل می‌کند؛ هنگام تغییر رمز مدیر نیز این کلید را عوض کنید. بازیابی رمز با ایمیل در این نسخه اضافه نشده است.

## اطلاعات و عکس‌ها

Netlify Blobs با دسترسی سروری، اطلاعات، حساب‌ها و عکس‌ها را نگه می‌دارد. برای این مسیر، D1، R2 یا اطلاعات کارت Cloudflare لازم نیست. اتصال Blobs در اجرای Netlify به‌صورت خودکار فراهم می‌شود و به Personal Access Token در متغیرها نیاز ندارد.

اطلاعات نسخه Sites به‌صورت خودکار منتقل نمی‌شود؛ این سایت یک فضای داده مستقل دارد. داده‌های نمونه با برچسب نمونه نمایش داده می‌شوند. پیام فرم تماس در صندوق ادمین ذخیره می‌شود؛ ارسال خودکار ایمیل انجام نمی‌شود.

این آداپتور برای تست و سایت کم‌ترافیک ساخته شده است: یک snapshot از SQLite را در Blobs نگه می‌دارد و برای جلوگیری از ازدست‌رفتن تغییرات هم‌زمان، از strong consistency و نوشتن مشروط ETag استفاده می‌کند. هر query snapshot را می‌خواند؛ بنابراین برای استفاده پرترافیک باید به پایگاه داده مدیریت‌شده مهاجرت کرد. سقف دفاعی snapshot در این نسخه ۳۲ مگابایت است و عکس‌ها جدا ذخیره می‌شوند. محدودیت‌های مصرف پلن Netlify همچنان اعمال می‌شوند.

## بررسی انتشار

- صفحه اصلی، معلم‌ها، بلاگ و تماس را باز کنید.
- با مدیر وارد شوید و ثبت پیام تماس را در `/admin` بررسی کنید.
- با دو حساب معلم، جدابودن داده‌ها، انتشار پرتفولیو و مقاله، دلار/تومان و بارگذاری عکس را آزمایش کنید.
- لینک عمومی را در مرورگر ناشناس باز کنید. اگر Netlify صفحه ورود خودش را نشان داد، برای انتشار عمومی پروژه گزینه **Make public** را انتخاب کنید.

## آزمون توسعه

```sh
npm ci
npm run build:netlify
node --test tests/netlify-storage.test.mjs
TEST_NETLIFY=1 node --test tests/netlify.test.mjs
```

آزمون بالا از سرور محلی رسمی Netlify Blobs و خروجی production واقعی Next استفاده می‌کند، به داده زنده یا اعتبارنامه واقعی دسترسی ندارد و سرورها و داده آزمایشی را پاک می‌کند. همچنین `npm test` ساخت و آزمون‌های مسیر Sites را اجرا می‌کند.
