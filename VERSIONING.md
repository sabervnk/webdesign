# نسخه‌بندی MissMahta

هر نسخه طراحی و کد در همین مخزن `sabervnk/webdesign` با یک تگ و یک GitHub Release جداگانه نگهداری می‌شود. صفحه نسخه‌ها:

https://github.com/sabervnk/webdesign/releases

نسخه پایه `v0.1.0` است. تا پایان بررسی نسخه آزمایشی، شماره اصلی صفر باقی می‌ماند.

## شماره نسخه

- اصلاح کوچک ظاهر یا رفع خطا: `0.1.0` ← `0.1.1`.
- مجموعه طراحی یا امکانات جدید: `0.1.1` ← `0.2.0`.
- نسخه اصلی پس از آمادگی و بررسی محصول: `1.0.0`.

شماره `package.json` و دو مقدار نسخه ریشه در `package-lock.json` باید یکسان باشند. نام تگ از افزودن `v` به این شماره ساخته می‌شود.

## ثبت نسخه بعدی

1. تغییرات را تکمیل و متناسب با آن‌ها بررسی کنید. برای تغییر کد سایت، بیلد هدف و آزمون‌های مرتبط را اجرا کنید.
2. شماره نسخه را در package.json و package-lock.json به شماره جدید افزایش دهید؛ برای نمونه `npm version 0.2.0 --no-git-tag-version`.
3. فایل جدیدی مانند `releases/v0.2.0.md` بسازید و تغییرات، نتیجه بررسی و محدودیت‌های نسخه را در آن بنویسید. وضعیت واقعی انتشار روی هاست را جداگانه مشخص کنید.
4. همه تغییرات نسخه را با هم روی شاخه `main` ذخیره کنید.
5. گردش‌کار `Publish versioned release` تگ را روی همان commit ثبت و Release را با متن فایل نسخه منتشر می‌کند. نتیجه را در Actions و Releases بررسی کنید.

این گردش‌کار فقط با تغییر فایل‌های توضیح نسخه یا package.json روی main، یا اجرای دستی از Actions آغاز می‌شود. تغییر عادی کد بدون آماده‌سازی یک نسخه جدید، به‌تنهایی Release تازه نمی‌سازد. برای اجرای دستی، شاخه main را انتخاب کنید.

تگ و Release موجود بازنویسی نمی‌شوند. برای اصلاح یک نسخه منتشرشده، شماره جدید بسازید. هر Release فایل‌های Source code با فرمت ZIP و tar.gz دارد و snapshot همان نسخه را نگه می‌دارد؛ لازم نیست کپی دیگری از پروژه یا مخزن جداگانه ایجاد شود.

## مشاهده یا بازگشت به نسخه

از صفحه Releases نسخه دلخواه را باز کرده و Source code را دانلود کنید. برای بررسی در Git از `git switch --detach v0.1.0` استفاده کنید و برای ادامه توسعه به `main` برگردید. بازگردانی سایت آنلاین نیاز به انتشار مجدد نسخه انتخابی روی هاست دارد.

Release فقط کد را نگه می‌دارد؛ اطلاعات کاربران، فایل‌های بارگذاری‌شده و تنظیمات محرمانه باید جداگانه پشتیبان‌گیری شوند. بازگشت کد به عقب، تغییرات پایگاه داده را خودکار برنمی‌گرداند.

گردش‌کار فقط مجوز `contents: write` برای ایجاد تگ و Release دارد و روی هاست استقرار انجام نمی‌دهد. اگر GitHub Actions در مخزن غیرفعال باشد یا مجوز نوشتن توسط سیاست حساب ممنوع باشد، ابتدا همان محدودیت باید رفع شود.
